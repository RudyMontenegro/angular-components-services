import { ChangeLetToNumPipe } from './change-let-to-num.pipe';

describe('ChangeLetToNumPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeLetToNumPipe();
    expect(pipe).toBeTruthy();
  });
});
